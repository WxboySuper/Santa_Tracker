import CustomProductsWorkspace from './CustomProductsWorkspace';
import './CustomProductsPage.css';

/** Local-only route shell kept intentionally small for lazy feature exposure. */
const CustomProductsPage = () => <CustomProductsWorkspace />;

export default CustomProductsPage;
