/**
 * Forecast Grade formula version.
 *
 * This identifier is embedded in every completed grade snapshot, share card, and
 * export so a stored grade can always be traced back to the exact math that
 * produced it. Bump this whenever a component formula or weight changes in a way
 * that would change historical grades.
 */
export const FORECAST_GRADE_FORMULA_VERSION = 'gfc-ver-1';
