{
  "name": "@santa-tracker/activity-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Activity lifecycle, inputs, saves, audio, achievements — mechanics-agnostic.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@santa-tracker/contracts": "workspace:*",
    "@santa-tracker/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.11"
  }
}

