{
  "name": "@santa-tracker/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Versioned Zod schemas and stable public identifiers — no DB, React, or env reads.",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas.ts",
    "./ids": "./src/ids.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.11"
  }
}

