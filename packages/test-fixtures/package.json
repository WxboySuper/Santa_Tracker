{
  "name": "@santa-tracker/test-fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic clocks, routes, snapshots, and content fixtures — dev-only.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@santa-tracker/contracts": "workspace:*",
    "@santa-tracker/route-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}

