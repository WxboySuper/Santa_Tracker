{
  "name": "@santa-tracker/route-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure route validation, compilation, state, and simulation — no Next.js, React, Drizzle, or global clock.",
  "exports": {
    ".": "./src/index.ts",
    "./validate": "./src/validate.ts",
    "./state": "./src/state.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@santa-tracker/contracts": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}

