server {
    listen 80;
    server_name telemetry.gfc.weatherboysuper.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}
